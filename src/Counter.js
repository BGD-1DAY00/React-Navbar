import React,{useState, useReducer} from 'react'

export const Counter = () => {
 
//   const [text, setText] = useState('');
//   const [items, setItems] = useState([]);
//   const [mess, setMess] = useState('');

//   const [name, setName] = useState('');
//   const reducerFunc =(state, action)=>{
//       switch(action){
//           case 'updateText': return {...state,[name]: value }; break;
//       }
//   }
//   const [state, dispatch] = useReducer(reducerFunc, {items: [], mess: ''})

//   const handleSub =(e)=>{
//     e.preventDefault()
//     if (name){

//     }
//     const name= e.target.name
//     const value = e.target.value
//     dispatch('updateText')
    
  
  return (
    <div>
    heelo
        {/* <div style={{marginTop: '3rem'}}>
            {state.mess}
        </div>

        <div>
            <input type="text" name='text' value={text} onChange={(e)=> setName(e.target.value)} />
            <button type='submit' onClick={handleSub}>Add</button>
        </div>

        <div>
            {state.items.map((each, index)=>{
                return(
                    <div key={index}>
                        <h1>{each}</h1>
                    </div>
                )
            })}
        </div> */}
    </div>
  )
}
